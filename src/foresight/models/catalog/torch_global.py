from __future__ import annotations

from typing import Any


def _make_wave31_training_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-patchtst-ema-global": (
            "torch-patchtst-global",
            "Torch PatchTST global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 60,
            },
        ),
        "torch-timesnet-swa-global": (
            "torch-timesnet-global",
            "Torch TimesNet global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-timexer-sam-global": (
            "torch-timexer-global",
            "Torch TimeXer global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-tsmixer-regularized-global": (
            "torch-tsmixer-global",
            "Torch TSMixer global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 50,
            },
        ),
        "torch-tft-longhorizon-global": (
            "torch-tft-global",
            "Torch TFT global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-seq2seq-attn-gru-lookahead-global": (
            "torch-seq2seq-attn-gru-global",
            "Torch Seq2Seq attention GRU global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave54_global_seq2seq_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-seq2seq-lstm-ema-global": (
            "torch-seq2seq-lstm-global",
            "Torch Seq2Seq LSTM global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-seq2seq-gru-swa-global": (
            "torch-seq2seq-gru-global",
            "Torch Seq2Seq GRU global forecaster with cosine plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-seq2seq-attn-lstm-sam-global": (
            "torch-seq2seq-attn-lstm-global",
            "Torch Seq2Seq attention LSTM global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-seq2seq-attn-gru-regularized-global": (
            "torch-seq2seq-attn-gru-global",
            "Torch Seq2Seq attention GRU global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-seq2seq-lstm-longhorizon-global": (
            "torch-seq2seq-lstm-global",
            "Torch Seq2Seq LSTM global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-seq2seq-attn-lstm-lookahead-global": (
            "torch-seq2seq-attn-lstm-global",
            "Torch Seq2Seq attention LSTM global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave56_global_transformer_encdec_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-transformer-encdec-ema-global": (
            "torch-transformer-encdec-global",
            "Torch global Transformer encoder-decoder forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-transformer-encdec-swa-global": (
            "torch-transformer-encdec-global",
            "Torch global Transformer encoder-decoder forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-transformer-encdec-sam-global": (
            "torch-transformer-encdec-global",
            "Torch global Transformer encoder-decoder forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-transformer-encdec-regularized-global": (
            "torch-transformer-encdec-global",
            "Torch global Transformer encoder-decoder forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-transformer-encdec-longhorizon-global": (
            "torch-transformer-encdec-global",
            "Torch global Transformer encoder-decoder forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-transformer-encdec-lookahead-global": (
            "torch-transformer-encdec-global",
            "Torch global Transformer encoder-decoder forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave57_global_lstnet_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-lstnet-ema-global": (
            "torch-lstnet-global",
            "Torch LSTNet global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-lstnet-swa-global": (
            "torch-lstnet-global",
            "Torch LSTNet global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-lstnet-sam-global": (
            "torch-lstnet-global",
            "Torch LSTNet global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-lstnet-regularized-global": (
            "torch-lstnet-global",
            "Torch LSTNet global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-lstnet-longhorizon-global": (
            "torch-lstnet-global",
            "Torch LSTNet global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-lstnet-lookahead-global": (
            "torch-lstnet-global",
            "Torch LSTNet global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave58_global_deepar_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-deepar-ema-global": (
            "torch-deepar-global",
            "Torch DeepAR global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-deepar-swa-global": (
            "torch-deepar-global",
            "Torch DeepAR global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-deepar-sam-global": (
            "torch-deepar-global",
            "Torch DeepAR global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-deepar-regularized-global": (
            "torch-deepar-global",
            "Torch DeepAR global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-deepar-longhorizon-global": (
            "torch-deepar-global",
            "Torch DeepAR global forecaster with horizon-weighted Gaussian training defaults for longer forecast windows. Requires PyTorch.",
            {
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-deepar-lookahead-global": (
            "torch-deepar-global",
            "Torch DeepAR global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave59_global_dlinear_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-dlinear-ema-global": (
            "torch-dlinear-global",
            "Torch DLinear global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-dlinear-swa-global": (
            "torch-dlinear-global",
            "Torch DLinear global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-dlinear-sam-global": (
            "torch-dlinear-global",
            "Torch DLinear global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-dlinear-regularized-global": (
            "torch-dlinear-global",
            "Torch DLinear global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-dlinear-longhorizon-global": (
            "torch-dlinear-global",
            "Torch DLinear global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-dlinear-lookahead-global": (
            "torch-dlinear-global",
            "Torch DLinear global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave60_global_nlinear_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-nlinear-ema-global": (
            "torch-nlinear-global",
            "Torch NLinear global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-nlinear-swa-global": (
            "torch-nlinear-global",
            "Torch NLinear global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-nlinear-sam-global": (
            "torch-nlinear-global",
            "Torch NLinear global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-nlinear-regularized-global": (
            "torch-nlinear-global",
            "Torch NLinear global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-nlinear-longhorizon-global": (
            "torch-nlinear-global",
            "Torch NLinear global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-nlinear-lookahead-global": (
            "torch-nlinear-global",
            "Torch NLinear global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave61_global_fnet_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-fnet-ema-global": (
            "torch-fnet-global",
            "Torch FNet global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-fnet-swa-global": (
            "torch-fnet-global",
            "Torch FNet global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-fnet-sam-global": (
            "torch-fnet-global",
            "Torch FNet global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-fnet-regularized-global": (
            "torch-fnet-global",
            "Torch FNet global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-fnet-longhorizon-global": (
            "torch-fnet-global",
            "Torch FNet global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-fnet-lookahead-global": (
            "torch-fnet-global",
            "Torch FNet global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave62_global_gmlp_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-gmlp-ema-global": (
            "torch-gmlp-global",
            "Torch gMLP global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-gmlp-swa-global": (
            "torch-gmlp-global",
            "Torch gMLP global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-gmlp-sam-global": (
            "torch-gmlp-global",
            "Torch gMLP global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-gmlp-regularized-global": (
            "torch-gmlp-global",
            "Torch gMLP global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-gmlp-longhorizon-global": (
            "torch-gmlp-global",
            "Torch gMLP global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-gmlp-lookahead-global": (
            "torch-gmlp-global",
            "Torch gMLP global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave63_global_ssm_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-ssm-ema-global": (
            "torch-ssm-global",
            "Torch SSM global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-ssm-swa-global": (
            "torch-ssm-global",
            "Torch SSM global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-ssm-sam-global": (
            "torch-ssm-global",
            "Torch SSM global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-ssm-regularized-global": (
            "torch-ssm-global",
            "Torch SSM global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-ssm-longhorizon-global": (
            "torch-ssm-global",
            "Torch SSM global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-ssm-lookahead-global": (
            "torch-ssm-global",
            "Torch SSM global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave64_global_mamba_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-mamba-ema-global": (
            "torch-mamba-global",
            "Torch Mamba global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-mamba-swa-global": (
            "torch-mamba-global",
            "Torch Mamba global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-mamba-sam-global": (
            "torch-mamba-global",
            "Torch Mamba global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-mamba-regularized-global": (
            "torch-mamba-global",
            "Torch Mamba global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-mamba-longhorizon-global": (
            "torch-mamba-global",
            "Torch Mamba global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-mamba-lookahead-global": (
            "torch-mamba-global",
            "Torch Mamba global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave65_global_rwkv_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-rwkv-ema-global": (
            "torch-rwkv-global",
            "Torch RWKV global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-rwkv-swa-global": (
            "torch-rwkv-global",
            "Torch RWKV global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-rwkv-sam-global": (
            "torch-rwkv-global",
            "Torch RWKV global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-rwkv-regularized-global": (
            "torch-rwkv-global",
            "Torch RWKV global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-rwkv-longhorizon-global": (
            "torch-rwkv-global",
            "Torch RWKV global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-rwkv-lookahead-global": (
            "torch-rwkv-global",
            "Torch RWKV global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave66_global_hyena_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-hyena-ema-global": (
            "torch-hyena-global",
            "Torch Hyena global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-hyena-swa-global": (
            "torch-hyena-global",
            "Torch Hyena global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-hyena-sam-global": (
            "torch-hyena-global",
            "Torch Hyena global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-hyena-regularized-global": (
            "torch-hyena-global",
            "Torch Hyena global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-hyena-longhorizon-global": (
            "torch-hyena-global",
            "Torch Hyena global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-hyena-lookahead-global": (
            "torch-hyena-global",
            "Torch Hyena global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave67_global_dilated_rnn_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-dilated-rnn-ema-global": (
            "torch-dilated-rnn-global",
            "Torch Dilated RNN global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-dilated-rnn-swa-global": (
            "torch-dilated-rnn-global",
            "Torch Dilated RNN global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-dilated-rnn-sam-global": (
            "torch-dilated-rnn-global",
            "Torch Dilated RNN global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-dilated-rnn-regularized-global": (
            "torch-dilated-rnn-global",
            "Torch Dilated RNN global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-dilated-rnn-longhorizon-global": (
            "torch-dilated-rnn-global",
            "Torch Dilated RNN global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-dilated-rnn-lookahead-global": (
            "torch-dilated-rnn-global",
            "Torch Dilated RNN global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave68_global_kan_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-kan-ema-global": (
            "torch-kan-global",
            "Torch KAN global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-kan-swa-global": (
            "torch-kan-global",
            "Torch KAN global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-kan-sam-global": (
            "torch-kan-global",
            "Torch KAN global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-kan-regularized-global": (
            "torch-kan-global",
            "Torch KAN global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-kan-longhorizon-global": (
            "torch-kan-global",
            "Torch KAN global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-kan-lookahead-global": (
            "torch-kan-global",
            "Torch KAN global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave69_global_scinet_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-scinet-ema-global": (
            "torch-scinet-global",
            "Torch SCINet global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-scinet-swa-global": (
            "torch-scinet-global",
            "Torch SCINet global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-scinet-sam-global": (
            "torch-scinet-global",
            "Torch SCINet global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-scinet-regularized-global": (
            "torch-scinet-global",
            "Torch SCINet global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-scinet-longhorizon-global": (
            "torch-scinet-global",
            "Torch SCINet global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-scinet-lookahead-global": (
            "torch-scinet-global",
            "Torch SCINet global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave70_global_etsformer_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-etsformer-ema-global": (
            "torch-etsformer-global",
            "Torch ETSformer global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-etsformer-swa-global": (
            "torch-etsformer-global",
            "Torch ETSformer global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-etsformer-sam-global": (
            "torch-etsformer-global",
            "Torch ETSformer global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-etsformer-regularized-global": (
            "torch-etsformer-global",
            "Torch ETSformer global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-etsformer-longhorizon-global": (
            "torch-etsformer-global",
            "Torch ETSformer global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-etsformer-lookahead-global": (
            "torch-etsformer-global",
            "Torch ETSformer global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave71_global_esrnn_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-esrnn-ema-global": (
            "torch-esrnn-global",
            "Torch ESRNN global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-esrnn-swa-global": (
            "torch-esrnn-global",
            "Torch ESRNN global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-esrnn-sam-global": (
            "torch-esrnn-global",
            "Torch ESRNN global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-esrnn-regularized-global": (
            "torch-esrnn-global",
            "Torch ESRNN global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-esrnn-longhorizon-global": (
            "torch-esrnn-global",
            "Torch ESRNN global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-esrnn-lookahead-global": (
            "torch-esrnn-global",
            "Torch ESRNN global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave72_global_tcn_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-tcn-ema-global": (
            "torch-tcn-global",
            "Torch TCN global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-tcn-swa-global": (
            "torch-tcn-global",
            "Torch TCN global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-tcn-sam-global": (
            "torch-tcn-global",
            "Torch TCN global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-tcn-regularized-global": (
            "torch-tcn-global",
            "Torch TCN global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-tcn-longhorizon-global": (
            "torch-tcn-global",
            "Torch TCN global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-tcn-lookahead-global": (
            "torch-tcn-global",
            "Torch TCN global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave73_global_nbeats_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-nbeats-ema-global": (
            "torch-nbeats-global",
            "Torch N-BEATS global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-nbeats-swa-global": (
            "torch-nbeats-global",
            "Torch N-BEATS global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-nbeats-sam-global": (
            "torch-nbeats-global",
            "Torch N-BEATS global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-nbeats-regularized-global": (
            "torch-nbeats-global",
            "Torch N-BEATS global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-nbeats-longhorizon-global": (
            "torch-nbeats-global",
            "Torch N-BEATS global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-nbeats-lookahead-global": (
            "torch-nbeats-global",
            "Torch N-BEATS global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave74_global_nhits_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-nhits-ema-global": (
            "torch-nhits-global",
            "Torch NHITS global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-nhits-swa-global": (
            "torch-nhits-global",
            "Torch NHITS global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-nhits-sam-global": (
            "torch-nhits-global",
            "Torch NHITS global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-nhits-regularized-global": (
            "torch-nhits-global",
            "Torch NHITS global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-nhits-longhorizon-global": (
            "torch-nhits-global",
            "Torch NHITS global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-nhits-lookahead-global": (
            "torch-nhits-global",
            "Torch NHITS global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave75_global_tide_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-tide-ema-global": (
            "torch-tide-global",
            "Torch TIDE global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-tide-swa-global": (
            "torch-tide-global",
            "Torch TIDE global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-tide-sam-global": (
            "torch-tide-global",
            "Torch TIDE global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-tide-regularized-global": (
            "torch-tide-global",
            "Torch TIDE global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-tide-longhorizon-global": (
            "torch-tide-global",
            "Torch TIDE global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-tide-lookahead-global": (
            "torch-tide-global",
            "Torch TIDE global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave76_global_wavenet_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-wavenet-ema-global": (
            "torch-wavenet-global",
            "Torch WaveNet global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-wavenet-swa-global": (
            "torch-wavenet-global",
            "Torch WaveNet global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-wavenet-sam-global": (
            "torch-wavenet-global",
            "Torch WaveNet global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-wavenet-regularized-global": (
            "torch-wavenet-global",
            "Torch WaveNet global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-wavenet-longhorizon-global": (
            "torch-wavenet-global",
            "Torch WaveNet global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-wavenet-lookahead-global": (
            "torch-wavenet-global",
            "Torch WaveNet global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave77_global_resnet1d_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-resnet1d-ema-global": (
            "torch-resnet1d-global",
            "Torch ResNet1D global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-resnet1d-swa-global": (
            "torch-resnet1d-global",
            "Torch ResNet1D global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-resnet1d-sam-global": (
            "torch-resnet1d-global",
            "Torch ResNet1D global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-resnet1d-regularized-global": (
            "torch-resnet1d-global",
            "Torch ResNet1D global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-resnet1d-longhorizon-global": (
            "torch-resnet1d-global",
            "Torch ResNet1D global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-resnet1d-lookahead-global": (
            "torch-resnet1d-global",
            "Torch ResNet1D global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave78_global_inception_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-inception-ema-global": (
            "torch-inception-global",
            "Torch Inception global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-inception-swa-global": (
            "torch-inception-global",
            "Torch Inception global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-inception-sam-global": (
            "torch-inception-global",
            "Torch Inception global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-inception-regularized-global": (
            "torch-inception-global",
            "Torch Inception global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-inception-longhorizon-global": (
            "torch-inception-global",
            "Torch Inception global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-inception-lookahead-global": (
            "torch-inception-global",
            "Torch Inception global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave79_global_fedformer_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-fedformer-ema-global": (
            "torch-fedformer-global",
            "Torch FEDformer global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-fedformer-swa-global": (
            "torch-fedformer-global",
            "Torch FEDformer global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-fedformer-sam-global": (
            "torch-fedformer-global",
            "Torch FEDformer global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-fedformer-regularized-global": (
            "torch-fedformer-global",
            "Torch FEDformer global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-fedformer-longhorizon-global": (
            "torch-fedformer-global",
            "Torch FEDformer global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-fedformer-lookahead-global": (
            "torch-fedformer-global",
            "Torch FEDformer global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave80_global_itransformer_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-itransformer-ema-global": (
            "torch-itransformer-global",
            "Torch iTransformer global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-itransformer-swa-global": (
            "torch-itransformer-global",
            "Torch iTransformer global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-itransformer-sam-global": (
            "torch-itransformer-global",
            "Torch iTransformer global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-itransformer-regularized-global": (
            "torch-itransformer-global",
            "Torch iTransformer global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-itransformer-longhorizon-global": (
            "torch-itransformer-global",
            "Torch iTransformer global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-itransformer-lookahead-global": (
            "torch-itransformer-global",
            "Torch iTransformer global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave81_global_informer_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-informer-ema-global": (
            "torch-informer-global",
            "Torch Informer global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-informer-swa-global": (
            "torch-informer-global",
            "Torch Informer global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-informer-sam-global": (
            "torch-informer-global",
            "Torch Informer global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-informer-regularized-global": (
            "torch-informer-global",
            "Torch Informer global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-informer-longhorizon-global": (
            "torch-informer-global",
            "Torch Informer global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-informer-lookahead-global": (
            "torch-informer-global",
            "Torch Informer global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave82_global_autoformer_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-autoformer-ema-global": (
            "torch-autoformer-global",
            "Torch Autoformer global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-autoformer-swa-global": (
            "torch-autoformer-global",
            "Torch Autoformer global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-autoformer-sam-global": (
            "torch-autoformer-global",
            "Torch Autoformer global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-autoformer-regularized-global": (
            "torch-autoformer-global",
            "Torch Autoformer global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-autoformer-longhorizon-global": (
            "torch-autoformer-global",
            "Torch Autoformer global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-autoformer-lookahead-global": (
            "torch-autoformer-global",
            "Torch Autoformer global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave83_global_nonstationary_transformer_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-nonstationary-transformer-ema-global": (
            "torch-nonstationary-transformer-global",
            "Torch Nonstationary Transformer global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-nonstationary-transformer-swa-global": (
            "torch-nonstationary-transformer-global",
            "Torch Nonstationary Transformer global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-nonstationary-transformer-sam-global": (
            "torch-nonstationary-transformer-global",
            "Torch Nonstationary Transformer global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-nonstationary-transformer-regularized-global": (
            "torch-nonstationary-transformer-global",
            "Torch Nonstationary Transformer global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-nonstationary-transformer-longhorizon-global": (
            "torch-nonstationary-transformer-global",
            "Torch Nonstationary Transformer global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-nonstationary-transformer-lookahead-global": (
            "torch-nonstationary-transformer-global",
            "Torch Nonstationary Transformer global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave84_global_crossformer_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-crossformer-ema-global": (
            "torch-crossformer-global",
            "Torch Crossformer global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-crossformer-swa-global": (
            "torch-crossformer-global",
            "Torch Crossformer global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-crossformer-sam-global": (
            "torch-crossformer-global",
            "Torch Crossformer global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-crossformer-regularized-global": (
            "torch-crossformer-global",
            "Torch Crossformer global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-crossformer-longhorizon-global": (
            "torch-crossformer-global",
            "Torch Crossformer global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-crossformer-lookahead-global": (
            "torch-crossformer-global",
            "Torch Crossformer global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave85_global_pyraformer_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-pyraformer-ema-global": (
            "torch-pyraformer-global",
            "Torch Pyraformer global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-pyraformer-swa-global": (
            "torch-pyraformer-global",
            "Torch Pyraformer global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-pyraformer-sam-global": (
            "torch-pyraformer-global",
            "Torch Pyraformer global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-pyraformer-regularized-global": (
            "torch-pyraformer-global",
            "Torch Pyraformer global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-pyraformer-longhorizon-global": (
            "torch-pyraformer-global",
            "Torch Pyraformer global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-pyraformer-lookahead-global": (
            "torch-pyraformer-global",
            "Torch Pyraformer global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave86_global_retnet_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-retnet-ema-global": (
            "torch-retnet-global",
            "Torch RetNet global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-retnet-swa-global": (
            "torch-retnet-global",
            "Torch RetNet global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-retnet-sam-global": (
            "torch-retnet-global",
            "Torch RetNet global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-retnet-regularized-global": (
            "torch-retnet-global",
            "Torch RetNet global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-retnet-longhorizon-global": (
            "torch-retnet-global",
            "Torch RetNet global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-retnet-lookahead-global": (
            "torch-retnet-global",
            "Torch RetNet global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave87_global_patchtst_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-patchtst-swa-global": (
            "torch-patchtst-global",
            "Torch PatchTST global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-patchtst-sam-global": (
            "torch-patchtst-global",
            "Torch PatchTST global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-patchtst-regularized-global": (
            "torch-patchtst-global",
            "Torch PatchTST global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-patchtst-longhorizon-global": (
            "torch-patchtst-global",
            "Torch PatchTST global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-patchtst-lookahead-global": (
            "torch-patchtst-global",
            "Torch PatchTST global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave88_global_timesnet_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-timesnet-ema-global": (
            "torch-timesnet-global",
            "Torch TimesNet global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-timesnet-sam-global": (
            "torch-timesnet-global",
            "Torch TimesNet global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-timesnet-regularized-global": (
            "torch-timesnet-global",
            "Torch TimesNet global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-timesnet-longhorizon-global": (
            "torch-timesnet-global",
            "Torch TimesNet global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-timesnet-lookahead-global": (
            "torch-timesnet-global",
            "Torch TimesNet global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave89_global_timexer_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-timexer-ema-global": (
            "torch-timexer-global",
            "Torch TimeXer global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-timexer-swa-global": (
            "torch-timexer-global",
            "Torch TimeXer global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-timexer-regularized-global": (
            "torch-timexer-global",
            "Torch TimeXer global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-timexer-longhorizon-global": (
            "torch-timexer-global",
            "Torch TimeXer global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-timexer-lookahead-global": (
            "torch-timexer-global",
            "Torch TimeXer global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave90_global_tsmixer_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-tsmixer-ema-global": (
            "torch-tsmixer-global",
            "Torch TSMixer global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-tsmixer-swa-global": (
            "torch-tsmixer-global",
            "Torch TSMixer global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-tsmixer-sam-global": (
            "torch-tsmixer-global",
            "Torch TSMixer global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-tsmixer-longhorizon-global": (
            "torch-tsmixer-global",
            "Torch TSMixer global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-tsmixer-lookahead-global": (
            "torch-tsmixer-global",
            "Torch TSMixer global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave91_global_tft_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-tft-ema-global": (
            "torch-tft-global",
            "Torch TFT global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-tft-swa-global": (
            "torch-tft-global",
            "Torch TFT global forecaster with cosine-restarts plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine_restarts",
                "scheduler_restart_period": 8,
                "scheduler_restart_mult": 2,
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-tft-sam-global": (
            "torch-tft-global",
            "Torch TFT global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-tft-regularized-global": (
            "torch-tft-global",
            "Torch TFT global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-tft-lookahead-global": (
            "torch-tft-global",
            "Torch TFT global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave92_global_seq2seq_attn_gru_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-seq2seq-attn-gru-ema-global": (
            "torch-seq2seq-attn-gru-global",
            "Torch Seq2Seq attention GRU global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-seq2seq-attn-gru-swa-global": (
            "torch-seq2seq-attn-gru-global",
            "Torch Seq2Seq attention GRU global forecaster with cosine plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-seq2seq-attn-gru-sam-global": (
            "torch-seq2seq-attn-gru-global",
            "Torch Seq2Seq attention GRU global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-seq2seq-attn-gru-longhorizon-global": (
            "torch-seq2seq-attn-gru-global",
            "Torch Seq2Seq attention GRU global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave93_global_seq2seq_attn_lstm_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-seq2seq-attn-lstm-ema-global": (
            "torch-seq2seq-attn-lstm-global",
            "Torch Seq2Seq attention LSTM global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-seq2seq-attn-lstm-swa-global": (
            "torch-seq2seq-attn-lstm-global",
            "Torch Seq2Seq attention LSTM global forecaster with cosine plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-seq2seq-attn-lstm-regularized-global": (
            "torch-seq2seq-attn-lstm-global",
            "Torch Seq2Seq attention LSTM global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-seq2seq-attn-lstm-longhorizon-global": (
            "torch-seq2seq-attn-lstm-global",
            "Torch Seq2Seq attention LSTM global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave94_global_seq2seq_lstm_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-seq2seq-lstm-swa-global": (
            "torch-seq2seq-lstm-global",
            "Torch Seq2Seq LSTM global forecaster with cosine plus SWA training recipe for flatter minima. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "weight_decay": 1e-4,
                "grad_clip_norm": 1.0,
                "swa_start_epoch": 18,
                "epochs": 32,
            },
        ),
        "torch-seq2seq-lstm-sam-global": (
            "torch-seq2seq-lstm-global",
            "Torch Seq2Seq LSTM global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-seq2seq-lstm-regularized-global": (
            "torch-seq2seq-lstm-global",
            "Torch Seq2Seq LSTM global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-seq2seq-lstm-lookahead-global": (
            "torch-seq2seq-lstm-global",
            "Torch Seq2Seq LSTM global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def _make_wave95_global_seq2seq_gru_strategy_presets(
    context: Any, catalog: dict[str, Any]
) -> dict[str, Any]:
    model_spec = context.ModelSpec
    extra: dict[str, Any] = {}

    preset_specs = {
        "torch-seq2seq-gru-ema-global": (
            "torch-seq2seq-gru-global",
            "Torch Seq2Seq GRU global forecaster with an EMA-stabilized cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "weight_decay": 1e-4,
                "ema_decay": 0.995,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-seq2seq-gru-sam-global": (
            "torch-seq2seq-gru-global",
            "Torch Seq2Seq GRU global forecaster with SAM plus cosine-warmup training recipe. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "sam_rho": 0.05,
                "sam_adaptive": True,
                "epochs": 36,
            },
        ),
        "torch-seq2seq-gru-regularized-global": (
            "torch-seq2seq-gru-global",
            "Torch Seq2Seq GRU global forecaster with dropout-heavy regularized training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 3,
                "min_lr": 1e-5,
                "weight_decay": 5e-4,
                "input_dropout": 0.1,
                "temporal_dropout": 0.05,
                "epochs": 40,
            },
        ),
        "torch-seq2seq-gru-longhorizon-global": (
            "torch-seq2seq-gru-global",
            "Torch Seq2Seq GRU global forecaster with long-horizon-weighted Huber training defaults. Requires PyTorch.",
            {
                "loss": "huber",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "horizon_loss_decay": 1.05,
                "ema_decay": 0.99,
                "ema_warmup_epochs": 1,
                "epochs": 40,
            },
        ),
        "torch-seq2seq-gru-lookahead-global": (
            "torch-seq2seq-gru-global",
            "Torch Seq2Seq GRU global forecaster with Lookahead-optimized cosine training defaults. Requires PyTorch.",
            {
                "optimizer": "adamw",
                "scheduler": "cosine",
                "warmup_epochs": 2,
                "min_lr": 1e-5,
                "grad_clip_norm": 1.0,
                "lookahead_steps": 5,
                "lookahead_alpha": 0.5,
                "epochs": 40,
            },
        ),
    }

    for key, (base_key, description, overrides) in preset_specs.items():
        base_spec = catalog[base_key]
        extra[key] = model_spec(
            key=key,
            description=description,
            factory=base_spec.factory,
            default_params={**base_spec.default_params, **overrides},
            param_help=dict(base_spec.param_help),
            requires=tuple(base_spec.requires),
            interface=str(base_spec.interface),
            capability_overrides=dict(base_spec.capability_overrides),
        )

    return extra


def build_torch_global_catalog(context: Any) -> dict[str, Any]:
    model_spec = context.ModelSpec
    _TORCH_COMMON_DEFAULTS = context._TORCH_COMMON_DEFAULTS
    _TORCH_COMMON_PARAM_HELP = context._TORCH_COMMON_PARAM_HELP
    _factory_torch_timexer_global = context._factory_torch_timexer_global
    torch_autoformer_global_forecaster = context.torch_autoformer_global_forecaster
    torch_crossformer_global_forecaster = context.torch_crossformer_global_forecaster
    torch_deepar_global_forecaster = context.torch_deepar_global_forecaster
    torch_dilated_rnn_global_forecaster = context.torch_dilated_rnn_global_forecaster
    torch_dlinear_global_forecaster = context.torch_dlinear_global_forecaster
    torch_esrnn_global_forecaster = context.torch_esrnn_global_forecaster
    torch_etsformer_global_forecaster = context.torch_etsformer_global_forecaster
    torch_fedformer_global_forecaster = context.torch_fedformer_global_forecaster
    torch_fnet_global_forecaster = context.torch_fnet_global_forecaster
    torch_gmlp_global_forecaster = context.torch_gmlp_global_forecaster
    torch_hyena_global_forecaster = context.torch_hyena_global_forecaster
    torch_inception_global_forecaster = context.torch_inception_global_forecaster
    torch_informer_global_forecaster = context.torch_informer_global_forecaster
    torch_itransformer_global_forecaster = context.torch_itransformer_global_forecaster
    torch_kan_global_forecaster = context.torch_kan_global_forecaster
    torch_lstnet_global_forecaster = context.torch_lstnet_global_forecaster
    torch_mamba_global_forecaster = context.torch_mamba_global_forecaster
    torch_nbeats_global_forecaster = context.torch_nbeats_global_forecaster
    torch_nhits_global_forecaster = context.torch_nhits_global_forecaster
    torch_nlinear_global_forecaster = context.torch_nlinear_global_forecaster
    torch_nonstationary_transformer_global_forecaster = context.torch_nonstationary_transformer_global_forecaster
    torch_patchtst_global_forecaster = context.torch_patchtst_global_forecaster
    torch_pyraformer_global_forecaster = context.torch_pyraformer_global_forecaster
    torch_resnet1d_global_forecaster = context.torch_resnet1d_global_forecaster
    torch_retnet_global_forecaster = context.torch_retnet_global_forecaster
    torch_rwkv_global_forecaster = context.torch_rwkv_global_forecaster
    torch_scinet_global_forecaster = context.torch_scinet_global_forecaster
    torch_seq2seq_global_forecaster = context.torch_seq2seq_global_forecaster
    torch_ssm_global_forecaster = context.torch_ssm_global_forecaster
    torch_tcn_global_forecaster = context.torch_tcn_global_forecaster
    torch_tft_global_forecaster = context.torch_tft_global_forecaster
    torch_tide_global_forecaster = context.torch_tide_global_forecaster
    torch_timesnet_global_forecaster = context.torch_timesnet_global_forecaster
    torch_transformer_encdec_global_forecaster = context.torch_transformer_encdec_global_forecaster
    torch_tsmixer_global_forecaster = context.torch_tsmixer_global_forecaster
    torch_wavenet_global_forecaster = context.torch_wavenet_global_forecaster
    CONTEXT_LENGTH_PARAM_HELP = "Context window length (encoder length)"
    OPTIONAL_X_COLS_PARAM_HELP = (
        "Optional covariate columns from long_df (comma-separated)"
    )
    ADD_TIME_FEATURES_PARAM_HELP = "Add built-in time features from ds (true/false)"
    SAMPLE_STEP_PARAM_HELP = "Stride when generating training windows (>=1)"
    ATTENTION_HEADS_PARAM_HELP = "Attention heads"
    MODEL_DIMENSION_PARAM_HELP = "Model dimension"
    TRANSFORMER_MODEL_DIMENSION_PARAM_HELP = "Transformer model dimension"
    ENCODER_LAYERS_PARAM_HELP = "Encoder layers"
    TRANSFORMER_FFN_DIM_PARAM_HELP = "Transformer FFN dimension"
    ID_EMB_DIM_PARAM_HELP = "Series-id embedding dim (panel/global models)"
    DROPOUT_PROBABILITY_PARAM_HELP = "Dropout probability in [0,1)"
    QUANTILES_PARAM_HELP = (
        "Optional quantiles for pinball loss, e.g. 0.1,0.5,0.9 (adds yhat_pXX columns)"
    )
    MAX_TRAIN_SIZE_PARAM_HELP = (
        "Optional per-series rolling training window length (None for expanding)"
    )
    CONV1D_KERNEL_SIZE_PARAM_HELP = "Conv1D kernel size (>=1)"
    NUM_RESIDUAL_BLOCKS_PARAM_HELP = "Number of residual blocks"
    RNN_HIDDEN_SIZE_PARAM_HELP = "RNN hidden size"
    STACKED_RNN_LAYERS_PARAM_HELP = "Number of stacked RNN layers"
    RNN_DROPOUT_PARAM_HELP = "Dropout probability in [0,1) (only if num_layers>1)"
    RNN_CELL_PARAM_HELP = "RNN cell: lstm, gru"
    ATTENTION_MODE_PARAM_HELP = "Attention: none, bahdanau"
    TEACHER_FORCING_PARAM_HELP = "Teacher forcing ratio at the start of training"
    TEACHER_FORCING_FINAL_PARAM_HELP = (
        "Teacher forcing ratio at the end of training (None keeps it constant)"
    )
    catalog = {
    "torch-tft-global": model_spec(
        key="torch-tft-global",
        description="Torch Temporal Fusion Transformer (lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_tft_global_forecaster,
        default_params={
            "context_length": 48,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "lstm_layers": 1,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": "Embedding dimension",
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "lstm_layers": "Number of LSTM layers",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-tft-deep-global": model_spec(
        key="torch-tft-deep-global",
        description="Torch Temporal Fusion Transformer, deeper config (2 LSTM layers) trained globally across panel series. Requires PyTorch.",
        factory=torch_tft_global_forecaster,
        default_params={
            "context_length": 48,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "lstm_layers": 2,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": "Embedding dimension",
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "lstm_layers": "Number of LSTM layers",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-tft-wide-global": model_spec(
        key="torch-tft-wide-global",
        description="Torch Temporal Fusion Transformer, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_tft_global_forecaster,
        default_params={
            "context_length": 48,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "nhead": 8,
            "lstm_layers": 1,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": "Embedding dimension",
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "lstm_layers": "Number of LSTM layers",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-timexer-global": model_spec(
        key="torch-timexer-global",
        description="Torch TimeXer-style exogenous-aware transformer (lite) trained globally across panel series. Requires PyTorch.",
        factory=_factory_torch_timexer_global,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "num_layers": 2,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": "Required covariate columns from long_df (comma-separated)",
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "nhead": "Number of attention heads",
            "num_layers": "Number of encoder / cross-attention blocks",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
        capability_overrides={"requires_future_covariates": True},
    ),
    "torch-timexer-deep-global": model_spec(
        key="torch-timexer-deep-global",
        description="Torch TimeXer-style exogenous-aware transformer, deeper config (4 blocks) trained globally across panel series. Requires PyTorch.",
        factory=_factory_torch_timexer_global,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "num_layers": 4,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": "Required covariate columns from long_df (comma-separated)",
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "nhead": "Number of attention heads",
            "num_layers": "Number of encoder / cross-attention blocks",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
        capability_overrides={"requires_future_covariates": True},
    ),
    "torch-timexer-wide-global": model_spec(
        key="torch-timexer-wide-global",
        description="Torch TimeXer-style exogenous-aware transformer, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=_factory_torch_timexer_global,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "nhead": 8,
            "num_layers": 2,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": "Required covariate columns from long_df (comma-separated)",
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "nhead": "Number of attention heads",
            "num_layers": "Number of encoder / cross-attention blocks",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
        capability_overrides={"requires_future_covariates": True},
    ),
    "torch-retnet-global": model_spec(
        key="torch-retnet-global",
        description="Torch RetNet-style retention network (lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_retnet_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "num_layers": 2,
            "ffn_dim": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "nhead": "Number of retention heads",
            "num_layers": "Number of stacked retention blocks",
            "ffn_dim": "Feed-forward hidden width inside each retention block",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-retnet-deep-global": model_spec(
        key="torch-retnet-deep-global",
        description="Torch RetNet-style retention network, deeper config (4 blocks) trained globally across panel series. Requires PyTorch.",
        factory=torch_retnet_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "num_layers": 4,
            "ffn_dim": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "nhead": "Number of retention heads",
            "num_layers": "Number of stacked retention blocks",
            "ffn_dim": "Feed-forward hidden width inside each retention block",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-retnet-wide-global": model_spec(
        key="torch-retnet-wide-global",
        description="Torch RetNet-style retention network, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_retnet_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "nhead": 8,
            "num_layers": 2,
            "ffn_dim": 512,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "nhead": "Number of retention heads",
            "num_layers": "Number of stacked retention blocks",
            "ffn_dim": "Feed-forward hidden width inside each retention block",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-informer-global": model_spec(
        key="torch-informer-global",
        description="Torch Informer (lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_informer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "num_layers": 2,
            "dim_feedforward": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": ENCODER_LAYERS_PARAM_HELP,
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-informer-deep-global": model_spec(
        key="torch-informer-deep-global",
        description="Torch Informer, deeper config (4 layers) trained globally across panel series. Requires PyTorch.",
        factory=torch_informer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "num_layers": 4,
            "dim_feedforward": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": ENCODER_LAYERS_PARAM_HELP,
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-informer-wide-global": model_spec(
        key="torch-informer-wide-global",
        description="Torch Informer, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_informer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "nhead": 8,
            "num_layers": 2,
            "dim_feedforward": 512,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": ENCODER_LAYERS_PARAM_HELP,
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-autoformer-global": model_spec(
        key="torch-autoformer-global",
        description="Torch Autoformer (lite) with series decomposition, trained globally across panel series. Requires PyTorch.",
        factory=torch_autoformer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "num_layers": 2,
            "dim_feedforward": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "ma_window": 7,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": ENCODER_LAYERS_PARAM_HELP,
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "ma_window": "Moving-average window for trend/seasonal decomposition",
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-autoformer-deep-global": model_spec(
        key="torch-autoformer-deep-global",
        description="Torch Autoformer with series decomposition, deeper config (4 layers) trained globally across panel series. Requires PyTorch.",
        factory=torch_autoformer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "num_layers": 4,
            "dim_feedforward": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "ma_window": 7,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": ENCODER_LAYERS_PARAM_HELP,
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "ma_window": "Moving-average window for trend/seasonal decomposition",
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-autoformer-wide-global": model_spec(
        key="torch-autoformer-wide-global",
        description="Torch Autoformer with series decomposition, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_autoformer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "nhead": 8,
            "num_layers": 2,
            "dim_feedforward": 512,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "ma_window": 7,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": ENCODER_LAYERS_PARAM_HELP,
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "ma_window": "Moving-average window for trend/seasonal decomposition",
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-fedformer-global": model_spec(
        key="torch-fedformer-global",
        description="Torch FEDformer-style (lite) decomposition + frequency mixing, trained globally across panel series. Requires PyTorch.",
        factory=torch_fedformer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "num_layers": 2,
            "ffn_dim": 256,
            "modes": 16,
            "ma_window": 7,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_layers": "Number of frequency-mixing blocks",
            "ffn_dim": "FFN hidden dimension",
            "modes": "Number of low-frequency FFT modes to keep (>=1)",
            "ma_window": "Moving-average window for trend extraction (>=1)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-fedformer-deep-global": model_spec(
        key="torch-fedformer-deep-global",
        description="Torch FEDformer-style decomposition + frequency mixing, deeper config (4 blocks) trained globally across panel series. Requires PyTorch.",
        factory=torch_fedformer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "num_layers": 4,
            "ffn_dim": 256,
            "modes": 16,
            "ma_window": 7,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_layers": "Number of frequency-mixing blocks",
            "ffn_dim": "FFN hidden dimension",
            "modes": "Number of low-frequency FFT modes to keep (>=1)",
            "ma_window": "Moving-average window for trend extraction (>=1)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-fedformer-wide-global": model_spec(
        key="torch-fedformer-wide-global",
        description="Torch FEDformer-style decomposition + frequency mixing, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_fedformer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "num_layers": 2,
            "ffn_dim": 512,
            "modes": 16,
            "ma_window": 7,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_layers": "Number of frequency-mixing blocks",
            "ffn_dim": "FFN hidden dimension",
            "modes": "Number of low-frequency FFT modes to keep (>=1)",
            "ma_window": "Moving-average window for trend extraction (>=1)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-nonstationary-transformer-global": model_spec(
        key="torch-nonstationary-transformer-global",
        description="Torch Non-stationary Transformer (lite) with RevIN + de-stationary attention factors, trained globally across panel series. Requires PyTorch.",
        factory=torch_nonstationary_transformer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "num_layers": 2,
            "dim_feedforward": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": ENCODER_LAYERS_PARAM_HELP,
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-nonstationary-transformer-deep-global": model_spec(
        key="torch-nonstationary-transformer-deep-global",
        description="Torch Non-stationary Transformer, deeper config (4 layers) trained globally across panel series. Requires PyTorch.",
        factory=torch_nonstationary_transformer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "num_layers": 4,
            "dim_feedforward": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": ENCODER_LAYERS_PARAM_HELP,
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-nonstationary-transformer-wide-global": model_spec(
        key="torch-nonstationary-transformer-wide-global",
        description="Torch Non-stationary Transformer, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_nonstationary_transformer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "nhead": 8,
            "num_layers": 2,
            "dim_feedforward": 512,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": ENCODER_LAYERS_PARAM_HELP,
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-patchtst-global": model_spec(
        key="torch-patchtst-global",
        description="Torch PatchTST-style (lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_patchtst_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "num_layers": 2,
            "dim_feedforward": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "patch_len": 16,
            "stride": 8,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": ENCODER_LAYERS_PARAM_HELP,
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "patch_len": "Patch length over (context+horizon) tokens",
            "stride": "Patch stride (>=1)",
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-patchtst-deep-global": model_spec(
        key="torch-patchtst-deep-global",
        description="Torch PatchTST-style, deeper config (4 layers) trained globally across panel series. Requires PyTorch.",
        factory=torch_patchtst_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "num_layers": 4,
            "dim_feedforward": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "patch_len": 16,
            "stride": 8,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": ENCODER_LAYERS_PARAM_HELP,
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "patch_len": "Patch length over (context+horizon) tokens",
            "stride": "Patch stride (>=1)",
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-patchtst-wide-global": model_spec(
        key="torch-patchtst-wide-global",
        description="Torch PatchTST-style, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_patchtst_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "nhead": 8,
            "num_layers": 2,
            "dim_feedforward": 512,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "patch_len": 16,
            "stride": 8,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": ENCODER_LAYERS_PARAM_HELP,
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "patch_len": "Patch length over (context+horizon) tokens",
            "stride": "Patch stride (>=1)",
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-crossformer-global": model_spec(
        key="torch-crossformer-global",
        description="Torch Crossformer-style (lite) multi-scale segmented Transformer encoder trained globally across panel series. Requires PyTorch.",
        factory=torch_crossformer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "num_layers": 2,
            "dim_feedforward": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "segment_len": 16,
            "stride": 16,
            "num_scales": 3,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": ENCODER_LAYERS_PARAM_HELP,
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "segment_len": "Base segment length over (context+horizon) tokens",
            "stride": "Base segment stride over (context+horizon) tokens",
            "num_scales": "Number of scales (>=1)",
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-crossformer-deep-global": model_spec(
        key="torch-crossformer-deep-global",
        description="Torch Crossformer-style segmented Transformer encoder, deeper config (4 layers) trained globally across panel series. Requires PyTorch.",
        factory=torch_crossformer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "num_layers": 4,
            "dim_feedforward": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "segment_len": 16,
            "stride": 16,
            "num_scales": 3,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": ENCODER_LAYERS_PARAM_HELP,
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "segment_len": "Base segment length over (context+horizon) tokens",
            "stride": "Base segment stride over (context+horizon) tokens",
            "num_scales": "Number of scales (>=1)",
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-crossformer-wide-global": model_spec(
        key="torch-crossformer-wide-global",
        description="Torch Crossformer-style segmented Transformer encoder, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_crossformer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "nhead": 8,
            "num_layers": 2,
            "dim_feedforward": 512,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "segment_len": 16,
            "stride": 16,
            "num_scales": 3,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": ENCODER_LAYERS_PARAM_HELP,
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "segment_len": "Base segment length over (context+horizon) tokens",
            "stride": "Base segment stride over (context+horizon) tokens",
            "num_scales": "Number of scales (>=1)",
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-pyraformer-global": model_spec(
        key="torch-pyraformer-global",
        description="Torch Pyraformer-style (lite) pyramid-pooled segmented Transformer encoder trained globally across panel series. Requires PyTorch.",
        factory=torch_pyraformer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "num_layers": 2,
            "dim_feedforward": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "segment_len": 16,
            "stride": 16,
            "num_levels": 3,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": ENCODER_LAYERS_PARAM_HELP,
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "segment_len": "Segment length at level-0 over (context+horizon) tokens",
            "stride": "Segment stride at level-0 over (context+horizon) tokens",
            "num_levels": "Number of pyramid levels (>=1)",
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-pyraformer-deep-global": model_spec(
        key="torch-pyraformer-deep-global",
        description="Torch Pyraformer-style pyramid-pooled segmented Transformer encoder, deeper config (4 layers) trained globally across panel series. Requires PyTorch.",
        factory=torch_pyraformer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "num_layers": 4,
            "dim_feedforward": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "segment_len": 16,
            "stride": 16,
            "num_levels": 3,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": ENCODER_LAYERS_PARAM_HELP,
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "segment_len": "Segment length at level-0 over (context+horizon) tokens",
            "stride": "Segment stride at level-0 over (context+horizon) tokens",
            "num_levels": "Number of pyramid levels (>=1)",
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-pyraformer-wide-global": model_spec(
        key="torch-pyraformer-wide-global",
        description="Torch Pyraformer-style pyramid-pooled segmented Transformer encoder, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_pyraformer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "nhead": 8,
            "num_layers": 2,
            "dim_feedforward": 512,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "segment_len": 16,
            "stride": 16,
            "num_levels": 3,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": ENCODER_LAYERS_PARAM_HELP,
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "segment_len": "Segment length at level-0 over (context+horizon) tokens",
            "stride": "Segment stride at level-0 over (context+horizon) tokens",
            "num_levels": "Number of pyramid levels (>=1)",
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-tsmixer-global": model_spec(
        key="torch-tsmixer-global",
        description="Torch TSMixer-style (lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_tsmixer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "num_blocks": 4,
            "token_mixing_hidden": 128,
            "channel_mixing_hidden": 128,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": "Mixer model dimension",
            "num_blocks": "Number of mixer blocks",
            "token_mixing_hidden": "Hidden size for token-mixing MLP",
            "channel_mixing_hidden": "Hidden size for channel-mixing MLP",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-tsmixer-deep-global": model_spec(
        key="torch-tsmixer-deep-global",
        description="Torch TSMixer-style, deeper config (6 blocks) trained globally across panel series. Requires PyTorch.",
        factory=torch_tsmixer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "num_blocks": 6,
            "token_mixing_hidden": 128,
            "channel_mixing_hidden": 128,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": "Mixer model dimension",
            "num_blocks": "Number of mixer blocks",
            "token_mixing_hidden": "Hidden size for token-mixing MLP",
            "channel_mixing_hidden": "Hidden size for channel-mixing MLP",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-tsmixer-wide-global": model_spec(
        key="torch-tsmixer-wide-global",
        description="Torch TSMixer-style, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_tsmixer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "num_blocks": 4,
            "token_mixing_hidden": 256,
            "channel_mixing_hidden": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": "Mixer model dimension",
            "num_blocks": "Number of mixer blocks",
            "token_mixing_hidden": "Hidden size for token-mixing MLP",
            "channel_mixing_hidden": "Hidden size for channel-mixing MLP",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-itransformer-global": model_spec(
        key="torch-itransformer-global",
        description="Torch iTransformer-style (lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_itransformer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "num_layers": 2,
            "dim_feedforward": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": ENCODER_LAYERS_PARAM_HELP,
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-itransformer-deep-global": model_spec(
        key="torch-itransformer-deep-global",
        description="Torch iTransformer-style, deeper config (4 layers) trained globally across panel series. Requires PyTorch.",
        factory=torch_itransformer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "num_layers": 4,
            "dim_feedforward": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": ENCODER_LAYERS_PARAM_HELP,
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-itransformer-wide-global": model_spec(
        key="torch-itransformer-wide-global",
        description="Torch iTransformer-style, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_itransformer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "nhead": 8,
            "num_layers": 2,
            "dim_feedforward": 512,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": ENCODER_LAYERS_PARAM_HELP,
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-timesnet-global": model_spec(
        key="torch-timesnet-global",
        description="Torch TimesNet-style (lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_timesnet_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "num_layers": 2,
            "top_k": 3,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": "Embedding dimension",
            "num_layers": "Number of TimesBlocks",
            "top_k": "Number of dominant periods to use (>=1)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-timesnet-deep-global": model_spec(
        key="torch-timesnet-deep-global",
        description="Torch TimesNet-style, deeper config (4 TimesBlocks) trained globally across panel series. Requires PyTorch.",
        factory=torch_timesnet_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "num_layers": 4,
            "top_k": 3,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": "Embedding dimension",
            "num_layers": "Number of TimesBlocks",
            "top_k": "Number of dominant periods to use (>=1)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-timesnet-wide-global": model_spec(
        key="torch-timesnet-wide-global",
        description="Torch TimesNet-style, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_timesnet_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "num_layers": 2,
            "top_k": 3,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": "Embedding dimension",
            "num_layers": "Number of TimesBlocks",
            "top_k": "Number of dominant periods to use (>=1)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-tcn-global": model_spec(
        key="torch-tcn-global",
        description="Torch TCN (causal dilated Conv1D) trained globally across panel series. Requires PyTorch.",
        factory=torch_tcn_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "channels": (64, 64, 64),
            "kernel_size": 3,
            "dilation_base": 2,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "channels": "Channel sizes for TCN layers (comma-separated)",
            "kernel_size": CONV1D_KERNEL_SIZE_PARAM_HELP,
            "dilation_base": "Dilation base (>=1), layer i uses dilation_base**i",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-tcn-deep-global": model_spec(
        key="torch-tcn-deep-global",
        description="Torch TCN, deeper config (4 conv blocks) trained globally across panel series. Requires PyTorch.",
        factory=torch_tcn_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "channels": (64, 64, 64, 64),
            "kernel_size": 3,
            "dilation_base": 2,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "channels": "Channel sizes for TCN layers (comma-separated)",
            "kernel_size": CONV1D_KERNEL_SIZE_PARAM_HELP,
            "dilation_base": "Dilation base (>=1), layer i uses dilation_base**i",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-tcn-wide-global": model_spec(
        key="torch-tcn-wide-global",
        description="Torch TCN, wider config (channels=128,128,128) trained globally across panel series. Requires PyTorch.",
        factory=torch_tcn_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "channels": (128, 128, 128),
            "kernel_size": 3,
            "dilation_base": 2,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "channels": "Channel sizes for TCN layers (comma-separated)",
            "kernel_size": CONV1D_KERNEL_SIZE_PARAM_HELP,
            "dilation_base": "Dilation base (>=1), layer i uses dilation_base**i",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-nbeats-global": model_spec(
        key="torch-nbeats-global",
        description="Torch N-BEATS-style (generic) trained globally across panel series. Requires PyTorch.",
        factory=torch_nbeats_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "num_blocks": 3,
            "num_layers": 2,
            "layer_width": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "num_blocks": NUM_RESIDUAL_BLOCKS_PARAM_HELP,
            "num_layers": "MLP layers per block",
            "layer_width": "Hidden width of MLP layers",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-nbeats-deep-global": model_spec(
        key="torch-nbeats-deep-global",
        description="Torch N-BEATS-style, deeper config (5 blocks) trained globally across panel series. Requires PyTorch.",
        factory=torch_nbeats_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "num_blocks": 5,
            "num_layers": 2,
            "layer_width": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "num_blocks": NUM_RESIDUAL_BLOCKS_PARAM_HELP,
            "num_layers": "MLP layers per block",
            "layer_width": "Hidden width of MLP layers",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-nbeats-wide-global": model_spec(
        key="torch-nbeats-wide-global",
        description="Torch N-BEATS-style, wider config (layer_width=512) trained globally across panel series. Requires PyTorch.",
        factory=torch_nbeats_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "num_blocks": 3,
            "num_layers": 2,
            "layer_width": 512,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "num_blocks": NUM_RESIDUAL_BLOCKS_PARAM_HELP,
            "num_layers": "MLP layers per block",
            "layer_width": "Hidden width of MLP layers",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-nhits-global": model_spec(
        key="torch-nhits-global",
        description="Torch N-HiTS-style (lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_nhits_global_forecaster,
        default_params={
            "context_length": 192,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "pool_sizes": (1, 2, 4),
            "num_blocks": 6,
            "num_layers": 2,
            "layer_width": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "pool_sizes": "Avg-pool downsample factors (comma-separated, <=context_length)",
            "num_blocks": NUM_RESIDUAL_BLOCKS_PARAM_HELP,
            "num_layers": "MLP layers per block",
            "layer_width": "Hidden width of MLP layers",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-nhits-deep-global": model_spec(
        key="torch-nhits-deep-global",
        description="Torch N-HiTS-style, deeper config (8 blocks) trained globally across panel series. Requires PyTorch.",
        factory=torch_nhits_global_forecaster,
        default_params={
            "context_length": 192,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "pool_sizes": (1, 2, 4),
            "num_blocks": 8,
            "num_layers": 2,
            "layer_width": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "pool_sizes": "Avg-pool downsample factors (comma-separated, <=context_length)",
            "num_blocks": NUM_RESIDUAL_BLOCKS_PARAM_HELP,
            "num_layers": "MLP layers per block",
            "layer_width": "Hidden width of MLP layers",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-nhits-wide-global": model_spec(
        key="torch-nhits-wide-global",
        description="Torch N-HiTS-style, wider config (layer_width=512) trained globally across panel series. Requires PyTorch.",
        factory=torch_nhits_global_forecaster,
        default_params={
            "context_length": 192,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "pool_sizes": (1, 2, 4),
            "num_blocks": 6,
            "num_layers": 2,
            "layer_width": 512,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "pool_sizes": "Avg-pool downsample factors (comma-separated, <=context_length)",
            "num_blocks": NUM_RESIDUAL_BLOCKS_PARAM_HELP,
            "num_layers": "MLP layers per block",
            "layer_width": "Hidden width of MLP layers",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-tide-global": model_spec(
        key="torch-tide-global",
        description="Torch TiDE-style (lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_tide_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "hidden_size": 128,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": "Latent model dimension",
            "hidden_size": "Hidden size for encoder/decoder MLPs",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-tide-long-global": model_spec(
        key="torch-tide-long-global",
        description="Torch TiDE-style, longer-context config (context_length=192) trained globally across panel series. Requires PyTorch.",
        factory=torch_tide_global_forecaster,
        default_params={
            "context_length": 192,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "hidden_size": 128,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": "Latent model dimension",
            "hidden_size": "Hidden size for encoder/decoder MLPs",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-tide-wide-global": model_spec(
        key="torch-tide-wide-global",
        description="Torch TiDE-style, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_tide_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "hidden_size": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": "Latent model dimension",
            "hidden_size": "Hidden size for encoder/decoder MLPs",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-wavenet-global": model_spec(
        key="torch-wavenet-global",
        description="Torch WaveNet-style gated dilated CNN trained globally across panel series. Requires PyTorch.",
        factory=torch_wavenet_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "channels": 32,
            "num_layers": 6,
            "kernel_size": 2,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "channels": "Hidden channels in WaveNet stack",
            "num_layers": "Number of dilated convolution layers",
            "kernel_size": CONV1D_KERNEL_SIZE_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-wavenet-deep-global": model_spec(
        key="torch-wavenet-deep-global",
        description="Torch WaveNet-style, deeper config (8 layers) trained globally across panel series. Requires PyTorch.",
        factory=torch_wavenet_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "channels": 32,
            "num_layers": 8,
            "kernel_size": 2,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "channels": "Hidden channels in WaveNet stack",
            "num_layers": "Number of dilated convolution layers",
            "kernel_size": CONV1D_KERNEL_SIZE_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-wavenet-wide-global": model_spec(
        key="torch-wavenet-wide-global",
        description="Torch WaveNet-style, wider config (channels=64) trained globally across panel series. Requires PyTorch.",
        factory=torch_wavenet_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "channels": 64,
            "num_layers": 6,
            "kernel_size": 2,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "channels": "Hidden channels in WaveNet stack",
            "num_layers": "Number of dilated convolution layers",
            "kernel_size": CONV1D_KERNEL_SIZE_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-resnet1d-global": model_spec(
        key="torch-resnet1d-global",
        description="Torch ResNet-1D Conv1D model trained globally across panel series. Requires PyTorch.",
        factory=torch_resnet1d_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "channels": 32,
            "num_blocks": 4,
            "kernel_size": 3,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "channels": "Conv1D channels",
            "num_blocks": NUM_RESIDUAL_BLOCKS_PARAM_HELP,
            "kernel_size": CONV1D_KERNEL_SIZE_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-resnet1d-deep-global": model_spec(
        key="torch-resnet1d-deep-global",
        description="Torch ResNet-1D, deeper config (6 residual blocks) trained globally across panel series. Requires PyTorch.",
        factory=torch_resnet1d_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "channels": 32,
            "num_blocks": 6,
            "kernel_size": 3,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "channels": "Conv1D channels",
            "num_blocks": NUM_RESIDUAL_BLOCKS_PARAM_HELP,
            "kernel_size": CONV1D_KERNEL_SIZE_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-resnet1d-wide-global": model_spec(
        key="torch-resnet1d-wide-global",
        description="Torch ResNet-1D, wider config (channels=64) trained globally across panel series. Requires PyTorch.",
        factory=torch_resnet1d_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "channels": 64,
            "num_blocks": 4,
            "kernel_size": 3,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "channels": "Conv1D channels",
            "num_blocks": NUM_RESIDUAL_BLOCKS_PARAM_HELP,
            "kernel_size": CONV1D_KERNEL_SIZE_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-inception-global": model_spec(
        key="torch-inception-global",
        description="Torch InceptionTime-style Conv1D model trained globally across panel series. Requires PyTorch.",
        factory=torch_inception_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "channels": 32,
            "num_blocks": 3,
            "kernel_sizes": (3, 5, 7),
            "bottleneck_channels": 16,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "channels": "Conv1D channels",
            "num_blocks": "Number of inception blocks",
            "kernel_sizes": "Kernel sizes (comma-separated)",
            "bottleneck_channels": "Bottleneck channels inside inception blocks",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-inception-deep-global": model_spec(
        key="torch-inception-deep-global",
        description="Torch InceptionTime-style Conv1D model, deeper config trained globally across panel series. Requires PyTorch.",
        factory=torch_inception_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "channels": 32,
            "num_blocks": 5,
            "kernel_sizes": (3, 5, 7),
            "bottleneck_channels": 16,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "channels": "Conv1D channels",
            "num_blocks": "Number of inception blocks",
            "kernel_sizes": "Kernel sizes (comma-separated)",
            "bottleneck_channels": "Bottleneck channels inside inception blocks",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-inception-wide-global": model_spec(
        key="torch-inception-wide-global",
        description="Torch InceptionTime-style Conv1D model, wider config trained globally across panel series. Requires PyTorch.",
        factory=torch_inception_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "channels": 64,
            "num_blocks": 3,
            "kernel_sizes": (3, 5, 7),
            "bottleneck_channels": 32,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "channels": "Conv1D channels",
            "num_blocks": "Number of inception blocks",
            "kernel_sizes": "Kernel sizes (comma-separated)",
            "bottleneck_channels": "Bottleneck channels inside inception blocks",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-lstnet-global": model_spec(
        key="torch-lstnet-global",
        description="Torch LSTNet-style (CNN+GRU+skip+highway, lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_lstnet_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "cnn_channels": 16,
            "kernel_size": 6,
            "rnn_hidden": 32,
            "skip": 24,
            "highway_window": 24,
            "id_emb_dim": 8,
            "dropout": 0.2,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "cnn_channels": "Conv1D output channels",
            "kernel_size": CONV1D_KERNEL_SIZE_PARAM_HELP,
            "rnn_hidden": "GRU hidden size",
            "skip": "Skip length for skip-GRU (0 disables)",
            "highway_window": "Highway window length over the context y channel (0 disables)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-lstnet-long-global": model_spec(
        key="torch-lstnet-long-global",
        description="Torch LSTNet-style, longer-context config (context_length=192) trained globally across panel series. Requires PyTorch.",
        factory=torch_lstnet_global_forecaster,
        default_params={
            "context_length": 192,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "cnn_channels": 16,
            "kernel_size": 6,
            "rnn_hidden": 32,
            "skip": 24,
            "highway_window": 24,
            "id_emb_dim": 8,
            "dropout": 0.2,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "cnn_channels": "Conv1D output channels",
            "kernel_size": CONV1D_KERNEL_SIZE_PARAM_HELP,
            "rnn_hidden": "GRU hidden size",
            "skip": "Skip length for skip-GRU (0 disables)",
            "highway_window": "Highway window length over the context y channel (0 disables)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-lstnet-wide-global": model_spec(
        key="torch-lstnet-wide-global",
        description="Torch LSTNet-style, wider config (cnn_channels=32, rnn_hidden=64) trained globally across panel series. Requires PyTorch.",
        factory=torch_lstnet_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "cnn_channels": 32,
            "kernel_size": 6,
            "rnn_hidden": 64,
            "skip": 24,
            "highway_window": 24,
            "id_emb_dim": 8,
            "dropout": 0.2,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "cnn_channels": "Conv1D output channels",
            "kernel_size": CONV1D_KERNEL_SIZE_PARAM_HELP,
            "rnn_hidden": "GRU hidden size",
            "skip": "Skip length for skip-GRU (0 disables)",
            "highway_window": "Highway window length over the context y channel (0 disables)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-fnet-global": model_spec(
        key="torch-fnet-global",
        description="Torch FNet-style (FFT token mixing, lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_fnet_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "num_layers": 4,
            "dim_feedforward": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_layers": "Number of FNet layers",
            "dim_feedforward": "FFN hidden dimension",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-fnet-deep-global": model_spec(
        key="torch-fnet-deep-global",
        description="Torch FNet-style, deeper config (6 layers) trained globally across panel series. Requires PyTorch.",
        factory=torch_fnet_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "num_layers": 6,
            "dim_feedforward": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_layers": "Number of FNet layers",
            "dim_feedforward": "FFN hidden dimension",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-fnet-wide-global": model_spec(
        key="torch-fnet-wide-global",
        description="Torch FNet-style, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_fnet_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "num_layers": 4,
            "dim_feedforward": 512,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_layers": "Number of FNet layers",
            "dim_feedforward": "FFN hidden dimension",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-gmlp-global": model_spec(
        key="torch-gmlp-global",
        description="Torch gMLP-style (spatial gating, lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_gmlp_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "num_layers": 4,
            "ffn_dim": 128,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_layers": "Number of gMLP layers",
            "ffn_dim": "FFN dimension inside gMLP",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-gmlp-deep-global": model_spec(
        key="torch-gmlp-deep-global",
        description="Torch gMLP-style, deeper config (6 layers) trained globally across panel series. Requires PyTorch.",
        factory=torch_gmlp_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "num_layers": 6,
            "ffn_dim": 128,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_layers": "Number of gMLP layers",
            "ffn_dim": "FFN dimension inside gMLP",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-gmlp-wide-global": model_spec(
        key="torch-gmlp-wide-global",
        description="Torch gMLP-style, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_gmlp_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "num_layers": 4,
            "ffn_dim": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_layers": "Number of gMLP layers",
            "ffn_dim": "FFN dimension inside gMLP",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-ssm-global": model_spec(
        key="torch-ssm-global",
        description="Torch diagonal state-space model (SSM, lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_ssm_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "num_layers": 4,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_layers": "Number of SSM blocks",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-ssm-deep-global": model_spec(
        key="torch-ssm-deep-global",
        description="Torch diagonal SSM, deeper config (6 blocks) trained globally across panel series. Requires PyTorch.",
        factory=torch_ssm_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "num_layers": 6,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_layers": "Number of SSM blocks",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-ssm-wide-global": model_spec(
        key="torch-ssm-wide-global",
        description="Torch diagonal SSM, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_ssm_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "num_layers": 4,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_layers": "Number of SSM blocks",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-mamba-global": model_spec(
        key="torch-mamba-global",
        description="Torch Mamba-style selective SSM (lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_mamba_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "num_layers": 4,
            "conv_kernel": 3,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_layers": "Number of stacked Mamba blocks",
            "conv_kernel": "Causal depthwise conv kernel size (>=1)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-mamba-deep-global": model_spec(
        key="torch-mamba-deep-global",
        description="Torch Mamba-style selective SSM, deeper config (6 blocks) trained globally across panel series. Requires PyTorch.",
        factory=torch_mamba_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "num_layers": 6,
            "conv_kernel": 3,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_layers": "Number of stacked Mamba blocks",
            "conv_kernel": "Causal depthwise conv kernel size (>=1)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-mamba-wide-global": model_spec(
        key="torch-mamba-wide-global",
        description="Torch Mamba-style selective SSM, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_mamba_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "num_layers": 4,
            "conv_kernel": 3,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_layers": "Number of stacked Mamba blocks",
            "conv_kernel": "Causal depthwise conv kernel size (>=1)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-rwkv-global": model_spec(
        key="torch-rwkv-global",
        description="Torch RWKV-style time-mix + channel-mix (lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_rwkv_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "num_layers": 4,
            "ffn_dim": 128,
            "id_emb_dim": 8,
            "dropout": 0.0,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_layers": "Number of stacked RWKV blocks",
            "ffn_dim": "Channel-mix hidden size",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-rwkv-deep-global": model_spec(
        key="torch-rwkv-deep-global",
        description="Torch RWKV-style time-mix + channel-mix, deeper config (6 blocks) trained globally across panel series. Requires PyTorch.",
        factory=torch_rwkv_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "num_layers": 6,
            "ffn_dim": 128,
            "id_emb_dim": 8,
            "dropout": 0.0,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_layers": "Number of stacked RWKV blocks",
            "ffn_dim": "Channel-mix hidden size",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-rwkv-wide-global": model_spec(
        key="torch-rwkv-wide-global",
        description="Torch RWKV-style time-mix + channel-mix, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_rwkv_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "num_layers": 4,
            "ffn_dim": 256,
            "id_emb_dim": 8,
            "dropout": 0.0,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_layers": "Number of stacked RWKV blocks",
            "ffn_dim": "Channel-mix hidden size",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-hyena-global": model_spec(
        key="torch-hyena-global",
        description="Torch Hyena-style long convolution (lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_hyena_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "num_layers": 4,
            "ffn_dim": 128,
            "kernel_size": 64,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_layers": "Number of Hyena blocks",
            "ffn_dim": "Channel-mixing FFN hidden size",
            "kernel_size": "Depthwise causal conv kernel size (>=1)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-hyena-deep-global": model_spec(
        key="torch-hyena-deep-global",
        description="Torch Hyena-style long convolution, deeper config (6 blocks) trained globally across panel series. Requires PyTorch.",
        factory=torch_hyena_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "num_layers": 6,
            "ffn_dim": 128,
            "kernel_size": 64,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_layers": "Number of Hyena blocks",
            "ffn_dim": "Channel-mixing FFN hidden size",
            "kernel_size": "Depthwise causal conv kernel size (>=1)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-hyena-wide-global": model_spec(
        key="torch-hyena-wide-global",
        description="Torch Hyena-style long convolution, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_hyena_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "num_layers": 4,
            "ffn_dim": 256,
            "kernel_size": 64,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_layers": "Number of Hyena blocks",
            "ffn_dim": "Channel-mixing FFN hidden size",
            "kernel_size": "Depthwise causal conv kernel size (>=1)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-dilated-rnn-global": model_spec(
        key="torch-dilated-rnn-global",
        description="Torch Dilated RNN (lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_dilated_rnn_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "cell": "gru",
            "d_model": 64,
            "num_layers": 3,
            "dilation_base": 2,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "cell": "Recurrent cell: gru or lstm",
            "d_model": "Hidden size / model dimension",
            "num_layers": "Number of dilated recurrent layers",
            "dilation_base": "Dilation base (>=2); dilations are base^layer_index",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-dilated-rnn-deep-global": model_spec(
        key="torch-dilated-rnn-deep-global",
        description="Torch Dilated RNN, deeper config (5 layers) trained globally across panel series. Requires PyTorch.",
        factory=torch_dilated_rnn_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "cell": "gru",
            "d_model": 64,
            "num_layers": 5,
            "dilation_base": 2,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "cell": "Recurrent cell: gru or lstm",
            "d_model": "Hidden size / model dimension",
            "num_layers": "Number of dilated recurrent layers",
            "dilation_base": "Dilation base (>=2); dilations are base^layer_index",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-dilated-rnn-wide-global": model_spec(
        key="torch-dilated-rnn-wide-global",
        description="Torch Dilated RNN, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_dilated_rnn_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "cell": "gru",
            "d_model": 128,
            "num_layers": 3,
            "dilation_base": 2,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "cell": "Recurrent cell: gru or lstm",
            "d_model": "Hidden size / model dimension",
            "num_layers": "Number of dilated recurrent layers",
            "dilation_base": "Dilation base (>=2); dilations are base^layer_index",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-kan-global": model_spec(
        key="torch-kan-global",
        description="Torch KAN-style spline MLP (lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_kan_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "num_layers": 2,
            "grid_size": 16,
            "grid_range": 2.0,
            "linear_skip": True,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": "Hidden width of the KAN network",
            "num_layers": "Number of KAN spline layers",
            "grid_size": "Number of spline knots (>=4)",
            "grid_range": "Spline grid range (+/- range) in normalized y units",
            "linear_skip": "Add a linear skip connection per layer (true/false)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-kan-deep-global": model_spec(
        key="torch-kan-deep-global",
        description="Torch KAN-style spline MLP, deeper config (4 spline layers) trained globally across panel series. Requires PyTorch.",
        factory=torch_kan_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "num_layers": 4,
            "grid_size": 16,
            "grid_range": 2.0,
            "linear_skip": True,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": "Hidden width of the KAN network",
            "num_layers": "Number of KAN spline layers",
            "grid_size": "Number of spline knots (>=4)",
            "grid_range": "Spline grid range (+/- range) in normalized y units",
            "linear_skip": "Add a linear skip connection per layer (true/false)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-kan-wide-global": model_spec(
        key="torch-kan-wide-global",
        description="Torch KAN-style spline MLP, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_kan_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "num_layers": 2,
            "grid_size": 16,
            "grid_range": 2.0,
            "linear_skip": True,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": "Hidden width of the KAN network",
            "num_layers": "Number of KAN spline layers",
            "grid_size": "Number of spline knots (>=4)",
            "grid_range": "Spline grid range (+/- range) in normalized y units",
            "linear_skip": "Add a linear skip connection per layer (true/false)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-scinet-global": model_spec(
        key="torch-scinet-global",
        description="Torch SCINet-style sample-convolution interaction network (lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_scinet_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "num_stages": 3,
            "conv_kernel": 5,
            "ffn_dim": 128,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_stages": "Number of SCINet interaction stages",
            "conv_kernel": "Conv1D kernel size (>=1) inside interaction blocks",
            "ffn_dim": "FFN hidden size inside blocks",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-scinet-deep-global": model_spec(
        key="torch-scinet-deep-global",
        description="Torch SCINet-style sample-convolution interaction network, deeper config (4 stages) trained globally across panel series. Requires PyTorch.",
        factory=torch_scinet_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "num_stages": 4,
            "conv_kernel": 5,
            "ffn_dim": 128,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_stages": "Number of SCINet interaction stages",
            "conv_kernel": "Conv1D kernel size (>=1) inside interaction blocks",
            "ffn_dim": "FFN hidden size inside blocks",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-scinet-wide-global": model_spec(
        key="torch-scinet-wide-global",
        description="Torch SCINet-style sample-convolution interaction network, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_scinet_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "num_stages": 3,
            "conv_kernel": 5,
            "ffn_dim": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": MODEL_DIMENSION_PARAM_HELP,
            "num_stages": "Number of SCINet interaction stages",
            "conv_kernel": "Conv1D kernel size (>=1) inside interaction blocks",
            "ffn_dim": "FFN hidden size inside blocks",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-etsformer-global": model_spec(
        key="torch-etsformer-global",
        description="Torch ETSformer-style exponential smoothing + Transformer residual model (lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_etsformer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "num_layers": 2,
            "dim_feedforward": 256,
            "dropout": 0.1,
            "alpha_init": 0.3,
            "beta_init": 0.1,
            "id_emb_dim": 8,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": "Transformer encoder layers",
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "alpha_init": "Initial smoothing alpha in (0,1) (learned during training)",
            "beta_init": "Initial smoothing beta in (0,1) (learned during training)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-etsformer-deep-global": model_spec(
        key="torch-etsformer-deep-global",
        description="Torch ETSformer-style exponential smoothing + Transformer residual model, deeper config (4 layers) trained globally across panel series. Requires PyTorch.",
        factory=torch_etsformer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "num_layers": 4,
            "dim_feedforward": 256,
            "dropout": 0.1,
            "alpha_init": 0.3,
            "beta_init": 0.1,
            "id_emb_dim": 8,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": "Transformer encoder layers",
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "alpha_init": "Initial smoothing alpha in (0,1) (learned during training)",
            "beta_init": "Initial smoothing beta in (0,1) (learned during training)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-etsformer-wide-global": model_spec(
        key="torch-etsformer-wide-global",
        description="Torch ETSformer-style exponential smoothing + Transformer residual model, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_etsformer_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "nhead": 8,
            "num_layers": 2,
            "dim_feedforward": 512,
            "dropout": 0.1,
            "alpha_init": 0.3,
            "beta_init": 0.1,
            "id_emb_dim": 8,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": "Transformer encoder layers",
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "alpha_init": "Initial smoothing alpha in (0,1) (learned during training)",
            "beta_init": "Initial smoothing beta in (0,1) (learned during training)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-esrnn-global": model_spec(
        key="torch-esrnn-global",
        description="Torch ESRNN-style hybrid (Holt smoothing + RNN residual, lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_esrnn_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "cell": "gru",
            "hidden_size": 64,
            "num_layers": 2,
            "dropout": 0.1,
            "alpha_init": 0.3,
            "beta_init": 0.1,
            "id_emb_dim": 8,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "cell": "RNN cell: gru or lstm",
            "hidden_size": RNN_HIDDEN_SIZE_PARAM_HELP,
            "num_layers": STACKED_RNN_LAYERS_PARAM_HELP,
            "dropout": RNN_DROPOUT_PARAM_HELP,
            "alpha_init": "Initial smoothing alpha in (0,1) (learned during training)",
            "beta_init": "Initial smoothing beta in (0,1) (learned during training)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-esrnn-deep-global": model_spec(
        key="torch-esrnn-deep-global",
        description="Torch ESRNN-style hybrid, deeper config (4 recurrent layers) trained globally across panel series. Requires PyTorch.",
        factory=torch_esrnn_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "cell": "gru",
            "hidden_size": 64,
            "num_layers": 4,
            "dropout": 0.1,
            "alpha_init": 0.3,
            "beta_init": 0.1,
            "id_emb_dim": 8,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "cell": "RNN cell: gru or lstm",
            "hidden_size": RNN_HIDDEN_SIZE_PARAM_HELP,
            "num_layers": STACKED_RNN_LAYERS_PARAM_HELP,
            "dropout": RNN_DROPOUT_PARAM_HELP,
            "alpha_init": "Initial smoothing alpha in (0,1) (learned during training)",
            "beta_init": "Initial smoothing beta in (0,1) (learned during training)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-esrnn-wide-global": model_spec(
        key="torch-esrnn-wide-global",
        description="Torch ESRNN-style hybrid, wider config (hidden_size=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_esrnn_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "cell": "gru",
            "hidden_size": 128,
            "num_layers": 2,
            "dropout": 0.1,
            "alpha_init": 0.3,
            "beta_init": 0.1,
            "id_emb_dim": 8,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "cell": "RNN cell: gru or lstm",
            "hidden_size": RNN_HIDDEN_SIZE_PARAM_HELP,
            "num_layers": STACKED_RNN_LAYERS_PARAM_HELP,
            "dropout": RNN_DROPOUT_PARAM_HELP,
            "alpha_init": "Initial smoothing alpha in (0,1) (learned during training)",
            "beta_init": "Initial smoothing beta in (0,1) (learned during training)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-transformer-encdec-global": model_spec(
        key="torch-transformer-encdec-global",
        description="Torch encoder-decoder Transformer (lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_transformer_encdec_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "num_layers": 2,
            "dim_feedforward": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": "Encoder/decoder layers",
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-transformer-encdec-deep-global": model_spec(
        key="torch-transformer-encdec-deep-global",
        description="Torch encoder-decoder Transformer, deeper config (4 layers) trained globally across panel series. Requires PyTorch.",
        factory=torch_transformer_encdec_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 64,
            "nhead": 4,
            "num_layers": 4,
            "dim_feedforward": 256,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": "Encoder/decoder layers",
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-transformer-encdec-wide-global": model_spec(
        key="torch-transformer-encdec-wide-global",
        description="Torch encoder-decoder Transformer, wider config (d_model=128) trained globally across panel series. Requires PyTorch.",
        factory=torch_transformer_encdec_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "d_model": 128,
            "nhead": 8,
            "num_layers": 2,
            "dim_feedforward": 512,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "d_model": TRANSFORMER_MODEL_DIMENSION_PARAM_HELP,
            "nhead": ATTENTION_HEADS_PARAM_HELP,
            "num_layers": "Encoder/decoder layers",
            "dim_feedforward": TRANSFORMER_FFN_DIM_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-nlinear-global": model_spec(
        key="torch-nlinear-global",
        description="Torch NLinear-style (last-value centering + linear head, lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_nlinear_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "id_emb_dim": 8,
            "dropout": 0.0,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-nlinear-long-global": model_spec(
        key="torch-nlinear-long-global",
        description="Torch NLinear-style, longer-context config (context_length=192) trained globally across panel series. Requires PyTorch.",
        factory=torch_nlinear_global_forecaster,
        default_params={
            "context_length": 192,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "id_emb_dim": 8,
            "dropout": 0.0,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-dlinear-global": model_spec(
        key="torch-dlinear-global",
        description="Torch DLinear-style (moving-average decomposition + linear heads, lite) trained globally across panel series. Requires PyTorch.",
        factory=torch_dlinear_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "ma_window": 7,
            "id_emb_dim": 8,
            "dropout": 0.0,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "ma_window": "Moving-average window size for trend extraction (>=1)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-dlinear-long-global": model_spec(
        key="torch-dlinear-long-global",
        description="Torch DLinear-style, longer-context config (context_length=192) trained globally across panel series. Requires PyTorch.",
        factory=torch_dlinear_global_forecaster,
        default_params={
            "context_length": 192,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "ma_window": 7,
            "id_emb_dim": 8,
            "dropout": 0.0,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "ma_window": "Moving-average window size for trend extraction (>=1)",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": DROPOUT_PROBABILITY_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-deepar-global": model_spec(
        key="torch-deepar-global",
        description="Torch DeepAR-style global Gaussian RNN (direct multi-horizon, lite). Requires PyTorch.",
        factory=torch_deepar_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "hidden_size": 64,
            "num_layers": 1,
            "id_emb_dim": 8,
            "dropout": 0.0,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "hidden_size": "GRU hidden size",
            "num_layers": "Number of GRU layers",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": RNN_DROPOUT_PARAM_HELP,
            "quantiles": "Optional quantiles (computed from Normal params; adds yhat_pXX columns)",
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
            "loss": "Ignored (DeepAR uses Gaussian NLL)",
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-deepar-deep-global": model_spec(
        key="torch-deepar-deep-global",
        description="Torch DeepAR-style global Gaussian RNN, deeper config (2 GRU layers). Requires PyTorch.",
        factory=torch_deepar_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "hidden_size": 64,
            "num_layers": 2,
            "id_emb_dim": 8,
            "dropout": 0.1,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "hidden_size": "GRU hidden size",
            "num_layers": "Number of GRU layers",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": RNN_DROPOUT_PARAM_HELP,
            "quantiles": "Optional quantiles (computed from Normal params; adds yhat_pXX columns)",
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-deepar-wide-global": model_spec(
        key="torch-deepar-wide-global",
        description="Torch DeepAR-style global Gaussian RNN, wider config (hidden_size=128). Requires PyTorch.",
        factory=torch_deepar_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "hidden_size": 128,
            "num_layers": 1,
            "id_emb_dim": 8,
            "dropout": 0.0,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "hidden_size": "GRU hidden size",
            "num_layers": "Number of GRU layers",
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "dropout": RNN_DROPOUT_PARAM_HELP,
            "quantiles": "Optional quantiles (computed from Normal params; adds yhat_pXX columns)",
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-seq2seq-lstm-global": model_spec(
        key="torch-seq2seq-lstm-global",
        description="Torch Seq2Seq (encoder-decoder) global model (LSTM, no attention). Requires PyTorch.",
        factory=torch_seq2seq_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "cell": "lstm",
            "attention": "none",
            "hidden_size": 64,
            "num_layers": 1,
            "dropout": 0.0,
            "id_emb_dim": 8,
            "teacher_forcing": 0.5,
            "teacher_forcing_final": None,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "cell": RNN_CELL_PARAM_HELP,
            "attention": ATTENTION_MODE_PARAM_HELP,
            "hidden_size": RNN_HIDDEN_SIZE_PARAM_HELP,
            "num_layers": STACKED_RNN_LAYERS_PARAM_HELP,
            "dropout": RNN_DROPOUT_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "teacher_forcing": TEACHER_FORCING_PARAM_HELP,
            "teacher_forcing_final": TEACHER_FORCING_FINAL_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-seq2seq-lstm-deep-global": model_spec(
        key="torch-seq2seq-lstm-deep-global",
        description="Torch Seq2Seq (encoder-decoder) global model, deeper config (LSTM, 2 layers, no attention). Requires PyTorch.",
        factory=torch_seq2seq_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "cell": "lstm",
            "attention": "none",
            "hidden_size": 64,
            "num_layers": 2,
            "dropout": 0.1,
            "id_emb_dim": 8,
            "teacher_forcing": 0.5,
            "teacher_forcing_final": None,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "cell": RNN_CELL_PARAM_HELP,
            "attention": ATTENTION_MODE_PARAM_HELP,
            "hidden_size": RNN_HIDDEN_SIZE_PARAM_HELP,
            "num_layers": STACKED_RNN_LAYERS_PARAM_HELP,
            "dropout": RNN_DROPOUT_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "teacher_forcing": TEACHER_FORCING_PARAM_HELP,
            "teacher_forcing_final": TEACHER_FORCING_FINAL_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-seq2seq-lstm-wide-global": model_spec(
        key="torch-seq2seq-lstm-wide-global",
        description="Torch Seq2Seq (encoder-decoder) global model, wider config (LSTM, hidden_size=128, no attention). Requires PyTorch.",
        factory=torch_seq2seq_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "cell": "lstm",
            "attention": "none",
            "hidden_size": 128,
            "num_layers": 1,
            "dropout": 0.0,
            "id_emb_dim": 8,
            "teacher_forcing": 0.5,
            "teacher_forcing_final": None,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "cell": RNN_CELL_PARAM_HELP,
            "attention": ATTENTION_MODE_PARAM_HELP,
            "hidden_size": RNN_HIDDEN_SIZE_PARAM_HELP,
            "num_layers": STACKED_RNN_LAYERS_PARAM_HELP,
            "dropout": RNN_DROPOUT_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "teacher_forcing": TEACHER_FORCING_PARAM_HELP,
            "teacher_forcing_final": TEACHER_FORCING_FINAL_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-seq2seq-gru-global": model_spec(
        key="torch-seq2seq-gru-global",
        description="Torch Seq2Seq (encoder-decoder) global model (GRU, no attention). Requires PyTorch.",
        factory=torch_seq2seq_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "cell": "gru",
            "attention": "none",
            "hidden_size": 64,
            "num_layers": 1,
            "dropout": 0.0,
            "id_emb_dim": 8,
            "teacher_forcing": 0.5,
            "teacher_forcing_final": None,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "cell": RNN_CELL_PARAM_HELP,
            "attention": ATTENTION_MODE_PARAM_HELP,
            "hidden_size": RNN_HIDDEN_SIZE_PARAM_HELP,
            "num_layers": STACKED_RNN_LAYERS_PARAM_HELP,
            "dropout": RNN_DROPOUT_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "teacher_forcing": TEACHER_FORCING_PARAM_HELP,
            "teacher_forcing_final": TEACHER_FORCING_FINAL_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-seq2seq-gru-deep-global": model_spec(
        key="torch-seq2seq-gru-deep-global",
        description="Torch Seq2Seq (encoder-decoder) global model, deeper config (GRU, 2 layers, no attention). Requires PyTorch.",
        factory=torch_seq2seq_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "cell": "gru",
            "attention": "none",
            "hidden_size": 64,
            "num_layers": 2,
            "dropout": 0.1,
            "id_emb_dim": 8,
            "teacher_forcing": 0.5,
            "teacher_forcing_final": None,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "cell": RNN_CELL_PARAM_HELP,
            "attention": ATTENTION_MODE_PARAM_HELP,
            "hidden_size": RNN_HIDDEN_SIZE_PARAM_HELP,
            "num_layers": STACKED_RNN_LAYERS_PARAM_HELP,
            "dropout": RNN_DROPOUT_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "teacher_forcing": TEACHER_FORCING_PARAM_HELP,
            "teacher_forcing_final": TEACHER_FORCING_FINAL_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-seq2seq-gru-wide-global": model_spec(
        key="torch-seq2seq-gru-wide-global",
        description="Torch Seq2Seq (encoder-decoder) global model, wider config (GRU, hidden_size=128, no attention). Requires PyTorch.",
        factory=torch_seq2seq_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "cell": "gru",
            "attention": "none",
            "hidden_size": 128,
            "num_layers": 1,
            "dropout": 0.0,
            "id_emb_dim": 8,
            "teacher_forcing": 0.5,
            "teacher_forcing_final": None,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "cell": RNN_CELL_PARAM_HELP,
            "attention": ATTENTION_MODE_PARAM_HELP,
            "hidden_size": RNN_HIDDEN_SIZE_PARAM_HELP,
            "num_layers": STACKED_RNN_LAYERS_PARAM_HELP,
            "dropout": RNN_DROPOUT_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "teacher_forcing": TEACHER_FORCING_PARAM_HELP,
            "teacher_forcing_final": TEACHER_FORCING_FINAL_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-seq2seq-attn-lstm-global": model_spec(
        key="torch-seq2seq-attn-lstm-global",
        description="Torch Seq2Seq (encoder-decoder) global model (LSTM + Bahdanau attention). Requires PyTorch.",
        factory=torch_seq2seq_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "cell": "lstm",
            "attention": "bahdanau",
            "hidden_size": 64,
            "num_layers": 1,
            "dropout": 0.0,
            "id_emb_dim": 8,
            "teacher_forcing": 0.5,
            "teacher_forcing_final": None,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "cell": RNN_CELL_PARAM_HELP,
            "attention": ATTENTION_MODE_PARAM_HELP,
            "hidden_size": RNN_HIDDEN_SIZE_PARAM_HELP,
            "num_layers": STACKED_RNN_LAYERS_PARAM_HELP,
            "dropout": RNN_DROPOUT_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "teacher_forcing": TEACHER_FORCING_PARAM_HELP,
            "teacher_forcing_final": TEACHER_FORCING_FINAL_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-seq2seq-attn-lstm-deep-global": model_spec(
        key="torch-seq2seq-attn-lstm-deep-global",
        description="Torch Seq2Seq (encoder-decoder) global model, deeper config (LSTM + Bahdanau attention, 2 layers). Requires PyTorch.",
        factory=torch_seq2seq_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "cell": "lstm",
            "attention": "bahdanau",
            "hidden_size": 64,
            "num_layers": 2,
            "dropout": 0.1,
            "id_emb_dim": 8,
            "teacher_forcing": 0.5,
            "teacher_forcing_final": None,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "cell": RNN_CELL_PARAM_HELP,
            "attention": ATTENTION_MODE_PARAM_HELP,
            "hidden_size": RNN_HIDDEN_SIZE_PARAM_HELP,
            "num_layers": STACKED_RNN_LAYERS_PARAM_HELP,
            "dropout": RNN_DROPOUT_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "teacher_forcing": TEACHER_FORCING_PARAM_HELP,
            "teacher_forcing_final": TEACHER_FORCING_FINAL_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-seq2seq-attn-lstm-wide-global": model_spec(
        key="torch-seq2seq-attn-lstm-wide-global",
        description="Torch Seq2Seq (encoder-decoder) global model, wider config (LSTM + Bahdanau attention, hidden_size=128). Requires PyTorch.",
        factory=torch_seq2seq_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "cell": "lstm",
            "attention": "bahdanau",
            "hidden_size": 128,
            "num_layers": 1,
            "dropout": 0.0,
            "id_emb_dim": 8,
            "teacher_forcing": 0.5,
            "teacher_forcing_final": None,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "cell": RNN_CELL_PARAM_HELP,
            "attention": ATTENTION_MODE_PARAM_HELP,
            "hidden_size": RNN_HIDDEN_SIZE_PARAM_HELP,
            "num_layers": STACKED_RNN_LAYERS_PARAM_HELP,
            "dropout": RNN_DROPOUT_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "teacher_forcing": TEACHER_FORCING_PARAM_HELP,
            "teacher_forcing_final": TEACHER_FORCING_FINAL_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-seq2seq-attn-gru-global": model_spec(
        key="torch-seq2seq-attn-gru-global",
        description="Torch Seq2Seq (encoder-decoder) global model (GRU + Bahdanau attention). Requires PyTorch.",
        factory=torch_seq2seq_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "cell": "gru",
            "attention": "bahdanau",
            "hidden_size": 64,
            "num_layers": 1,
            "dropout": 0.0,
            "id_emb_dim": 8,
            "teacher_forcing": 0.5,
            "teacher_forcing_final": None,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "cell": RNN_CELL_PARAM_HELP,
            "attention": ATTENTION_MODE_PARAM_HELP,
            "hidden_size": RNN_HIDDEN_SIZE_PARAM_HELP,
            "num_layers": STACKED_RNN_LAYERS_PARAM_HELP,
            "dropout": RNN_DROPOUT_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "teacher_forcing": TEACHER_FORCING_PARAM_HELP,
            "teacher_forcing_final": TEACHER_FORCING_FINAL_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-seq2seq-attn-gru-deep-global": model_spec(
        key="torch-seq2seq-attn-gru-deep-global",
        description="Torch Seq2Seq (encoder-decoder) global model, deeper config (GRU + Bahdanau attention, 2 layers). Requires PyTorch.",
        factory=torch_seq2seq_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "cell": "gru",
            "attention": "bahdanau",
            "hidden_size": 64,
            "num_layers": 2,
            "dropout": 0.1,
            "id_emb_dim": 8,
            "teacher_forcing": 0.5,
            "teacher_forcing_final": None,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "cell": RNN_CELL_PARAM_HELP,
            "attention": ATTENTION_MODE_PARAM_HELP,
            "hidden_size": RNN_HIDDEN_SIZE_PARAM_HELP,
            "num_layers": STACKED_RNN_LAYERS_PARAM_HELP,
            "dropout": RNN_DROPOUT_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "teacher_forcing": TEACHER_FORCING_PARAM_HELP,
            "teacher_forcing_final": TEACHER_FORCING_FINAL_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    "torch-seq2seq-attn-gru-wide-global": model_spec(
        key="torch-seq2seq-attn-gru-wide-global",
        description="Torch Seq2Seq (encoder-decoder) global model, wider config (GRU + Bahdanau attention, hidden_size=128). Requires PyTorch.",
        factory=torch_seq2seq_global_forecaster,
        default_params={
            "context_length": 96,
            "x_cols": (),
            "add_time_features": True,
            "sample_step": 1,
            "cell": "gru",
            "attention": "bahdanau",
            "hidden_size": 128,
            "num_layers": 1,
            "dropout": 0.0,
            "id_emb_dim": 8,
            "teacher_forcing": 0.5,
            "teacher_forcing_final": None,
            "quantiles": (),
            "max_train_size": None,
            **_TORCH_COMMON_DEFAULTS,
            "epochs": 30,
            "batch_size": 64,
            "val_split": 0.1,
        },
        param_help={
            "context_length": CONTEXT_LENGTH_PARAM_HELP,
            "x_cols": OPTIONAL_X_COLS_PARAM_HELP,
            "add_time_features": ADD_TIME_FEATURES_PARAM_HELP,
            "sample_step": SAMPLE_STEP_PARAM_HELP,
            "cell": RNN_CELL_PARAM_HELP,
            "attention": ATTENTION_MODE_PARAM_HELP,
            "hidden_size": RNN_HIDDEN_SIZE_PARAM_HELP,
            "num_layers": STACKED_RNN_LAYERS_PARAM_HELP,
            "dropout": RNN_DROPOUT_PARAM_HELP,
            "id_emb_dim": ID_EMB_DIM_PARAM_HELP,
            "teacher_forcing": TEACHER_FORCING_PARAM_HELP,
            "teacher_forcing_final": TEACHER_FORCING_FINAL_PARAM_HELP,
            "quantiles": QUANTILES_PARAM_HELP,
            "max_train_size": MAX_TRAIN_SIZE_PARAM_HELP,
            **_TORCH_COMMON_PARAM_HELP,
        },
        requires=("torch",),
        interface="global",
    ),
    }
    catalog.update(_make_wave31_training_strategy_presets(context, catalog))
    catalog.update(_make_wave54_global_seq2seq_strategy_presets(context, catalog))
    catalog.update(_make_wave56_global_transformer_encdec_strategy_presets(context, catalog))
    catalog.update(_make_wave57_global_lstnet_strategy_presets(context, catalog))
    catalog.update(_make_wave58_global_deepar_strategy_presets(context, catalog))
    catalog.update(_make_wave59_global_dlinear_strategy_presets(context, catalog))
    catalog.update(_make_wave60_global_nlinear_strategy_presets(context, catalog))
    catalog.update(_make_wave61_global_fnet_strategy_presets(context, catalog))
    catalog.update(_make_wave62_global_gmlp_strategy_presets(context, catalog))
    catalog.update(_make_wave63_global_ssm_strategy_presets(context, catalog))
    catalog.update(_make_wave64_global_mamba_strategy_presets(context, catalog))
    catalog.update(_make_wave65_global_rwkv_strategy_presets(context, catalog))
    catalog.update(_make_wave66_global_hyena_strategy_presets(context, catalog))
    catalog.update(_make_wave67_global_dilated_rnn_strategy_presets(context, catalog))
    catalog.update(_make_wave68_global_kan_strategy_presets(context, catalog))
    catalog.update(_make_wave69_global_scinet_strategy_presets(context, catalog))
    catalog.update(_make_wave70_global_etsformer_strategy_presets(context, catalog))
    catalog.update(_make_wave71_global_esrnn_strategy_presets(context, catalog))
    catalog.update(_make_wave72_global_tcn_strategy_presets(context, catalog))
    catalog.update(_make_wave73_global_nbeats_strategy_presets(context, catalog))
    catalog.update(_make_wave74_global_nhits_strategy_presets(context, catalog))
    catalog.update(_make_wave75_global_tide_strategy_presets(context, catalog))
    catalog.update(_make_wave76_global_wavenet_strategy_presets(context, catalog))
    catalog.update(_make_wave77_global_resnet1d_strategy_presets(context, catalog))
    catalog.update(_make_wave78_global_inception_strategy_presets(context, catalog))
    catalog.update(_make_wave79_global_fedformer_strategy_presets(context, catalog))
    catalog.update(_make_wave80_global_itransformer_strategy_presets(context, catalog))
    catalog.update(_make_wave81_global_informer_strategy_presets(context, catalog))
    catalog.update(_make_wave82_global_autoformer_strategy_presets(context, catalog))
    catalog.update(_make_wave83_global_nonstationary_transformer_strategy_presets(context, catalog))
    catalog.update(_make_wave84_global_crossformer_strategy_presets(context, catalog))
    catalog.update(_make_wave85_global_pyraformer_strategy_presets(context, catalog))
    catalog.update(_make_wave86_global_retnet_strategy_presets(context, catalog))
    catalog.update(_make_wave87_global_patchtst_strategy_presets(context, catalog))
    catalog.update(_make_wave88_global_timesnet_strategy_presets(context, catalog))
    catalog.update(_make_wave89_global_timexer_strategy_presets(context, catalog))
    catalog.update(_make_wave90_global_tsmixer_strategy_presets(context, catalog))
    catalog.update(_make_wave91_global_tft_strategy_presets(context, catalog))
    catalog.update(_make_wave92_global_seq2seq_attn_gru_strategy_presets(context, catalog))
    catalog.update(_make_wave93_global_seq2seq_attn_lstm_strategy_presets(context, catalog))
    catalog.update(_make_wave94_global_seq2seq_lstm_strategy_presets(context, catalog))
    catalog.update(_make_wave95_global_seq2seq_gru_strategy_presets(context, catalog))
    return catalog
